# TanStack Start (Nitro netlify preset) static chiqishni `dist` ga yozadi,
# server funksiya esa avtomatik `.netlify/functions-internal` dan olinadi.
[build]
  command = "pnpm run build"
  publish = "dist"
