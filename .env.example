# kvarts-landing .env.example
# Nusxalash: cp .env.example .env.local

# kvarts-uz-backend API base (SSR loader shu yerdan /public/settings oladi).
# Backend ishlamasa sahifa statik defaultlarni ko'rsatadi.
VITE_API_URL=http://localhost:3000/api/v1
